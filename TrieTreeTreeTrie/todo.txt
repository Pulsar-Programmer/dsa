TEST ALL METHODS
3. Display the tree ( Each Node can display it’s children) line by line toString() -> TEST TO STRING MORE RESPONSIVELY
5. Autocomplete (show all possible ways to finish from a prefix, if a prefix doesn’t exist say “no
such word”) auto(String) -> On frontend
TEST OVERALL FUNCTIONALITY
5: print inorder