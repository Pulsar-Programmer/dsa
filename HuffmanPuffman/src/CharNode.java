/** CharNode (this will have the char, priority, frequency and code) */
public class CharNode {
    public char character;
    /** This will act as the priority. */
    public int frequency;
    public int code;
    public CharNode left, right;
}
